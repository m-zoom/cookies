[project]
name = "cookie"
version = "0.1.0"
description = "cookie"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "PythonForWindows",
    "cryptography",
    "playwright",
    "requests>=2.32.5",
]

[tool.uv]
package = false

[dependency-groups]
dev = [
    "pyinstaller>=6.21.0",
]
